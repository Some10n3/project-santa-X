import React from 'react';

const Header: React.FC = () => (
  <header style={{ padding: '1rem', background: '#222', textAlign: 'center' }}>
    <h1>Project Santa X</h1>
  </header>
);

export default Header;